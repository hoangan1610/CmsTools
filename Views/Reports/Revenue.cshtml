@model RevenueReportViewModel
@using CmsTools.Models
@using System.Globalization
@using System.Text.Json
@using System
@using System.Collections.Generic
@using System.Linq

@{
    ViewData["Title"] = "Báo cáo doanh thu";

    var hasRows = Model.Rows != null && Model.Rows.Any();

    // ===== Range days =====
    var rangeDays = (Model.ToDate.Date - Model.FromDate.Date).Days + 1;
    if (rangeDays < 1) rangeDays = 1;

    // ===== KPI nâng cao =====
    var periodCount = hasRows ? Model.Rows.Count : 0;
    var periodLabel = (Model.GroupBy == "month") ? "Số tháng có đơn" : "Số ngày có đơn";

    var avgRevenuePerDay = (rangeDays > 0)
        ? (Model.Overview.TotalPayTotal / rangeDays)
        : 0m;

    var discountRate = (Model.Overview.TotalSubTotal > 0)
        ? (Model.Overview.TotalDiscountTotal / Model.Overview.TotalSubTotal * 100m)
        : 0m;

    var revDelta = Model.RevenueChangePercent;
    var ordDelta = Model.OrdersChangePercent;

    string RevDeltaCss =
        revDelta > 0 ? "text-success" :
        revDelta < 0 ? "text-danger" : "text-muted";

    string OrdDeltaCss =
        ordDelta > 0 ? "text-success" :
        ordDelta < 0 ? "text-danger" : "text-muted";

    var revDeltaText = revDelta == 0
        ? "0 %"
        : revDelta.ToString("0.##", CultureInfo.CurrentCulture) + " %";

    var ordDeltaText = ordDelta == 0
        ? "0 %"
        : ordDelta.ToString("0.##", CultureInfo.CurrentCulture) + " %";

    // ===== Data cho Chart.js (xu hướng doanh thu ghi nhận) =====
    var labelFormat = Model.GroupBy == "month" ? "MM/yyyy" : "dd/MM/yyyy";
    var labels = hasRows
        ? Model.Rows.Select(r => r.PeriodDate.ToString(labelFormat)).ToList()
        : new List<string>();

    var revenueData = hasRows
        ? Model.Rows.Select(r => r.PayTotal).ToList()
        : new List<decimal>();

    var ordersData = hasRows
        ? Model.Rows.Select(r => r.OrderCount).ToList()
        : new List<int>();

    var labelsJson = JsonSerializer.Serialize(labels);
    var revenueJson = JsonSerializer.Serialize(revenueData);
    var ordersJson = JsonSerializer.Serialize(ordersData);

    // ===== Top 5 period doanh thu cao nhất =====
    var top5RevenueDays = hasRows
        ? Model.Rows
            .OrderByDescending(r => r.PayTotal)
            .Take(5)
            .ToList()
        : new List<RevenueByPeriodRow>();

    var topPeriodDates = new HashSet<DateTime>(top5RevenueDays.Select(r => r.PeriodDate.Date));

    // ===== Data kênh thanh toán (doanh thu ghi nhận) =====
    var providerRows = Model.Providers ?? new List<RevenueByProviderRow>();
    var hasProviderRows = providerRows.Any();

    var providerDisplayLabels = providerRows
        .Select(p => $"{p.ProviderLabel} ({p.MethodLabel})")
        .ToList();

    var providerAmounts = providerRows.Select(p => p.TotalAmount).ToList();
    var providerOrders = providerRows.Select(p => p.SuccessfulOrders).ToList();

    var providerLabelsJson = JsonSerializer.Serialize(providerDisplayLabels);
    var providerAmountsJson = JsonSerializer.Serialize(providerAmounts);
    var providerOrdersJson = JsonSerializer.Serialize(providerOrders);

    // ===== Tổng hợp COD vs Online (doanh thu ghi nhận) =====
    var totalRevenue = Model.Overview.TotalPayTotal;
    var totalOrders = Model.Overview.TotalOrders;

    int codOrders = 0;
    decimal codAmount = 0m;
    int onlineOrders = 0;
    decimal onlineAmount = 0m;

    bool IsCod(RevenueByProviderRow p) =>
        string.Equals((p.ProviderLabel ?? "").Trim(), "COD", StringComparison.OrdinalIgnoreCase);

    if (hasProviderRows)
    {
        codOrders = providerRows.Where(IsCod).Sum(p => p.SuccessfulOrders);
        codAmount = providerRows.Where(IsCod).Sum(p => p.TotalAmount);

        onlineOrders = providerRows.Where(p => !IsCod(p)).Sum(p => p.SuccessfulOrders);
        onlineAmount = providerRows.Where(p => !IsCod(p)).Sum(p => p.TotalAmount);
    }

    var onlineRevenueShare = (totalRevenue > 0m && onlineAmount > 0m)
        ? onlineAmount * 100m / totalRevenue
        : 0m;

    var onlineOrderShare = (totalOrders > 0 && onlineOrders > 0)
        ? (decimal)onlineOrders * 100m / totalOrders
        : 0m;

    // ===== Đối soát: tổng theo kênh vs overview =====
    decimal providersSumAmount = hasProviderRows ? providerRows.Sum(p => p.TotalAmount) : 0m;
    int providersSumOrders = hasProviderRows ? providerRows.Sum(p => p.SuccessfulOrders) : 0;

    decimal providersAmountDiff = providersSumAmount - totalRevenue;
    int providersOrdersDiff = providersSumOrders - totalOrders;

    bool providersMismatch =
        hasProviderRows &&
        (Math.Abs(providersAmountDiff) >= 1m || providersOrdersDiff != 0);

    // ==========================================================
    // ===== DÒNG TIỀN (tiền vào) =====
    // ==========================================================
    var cashOv = Model.CashflowOverview ?? new CashflowOverview();
    var cashRows = Model.CashflowRows ?? new List<CashflowByPeriodRow>();
    var hasCashRows = cashRows.Any();

    var cashLabels = hasCashRows
        ? cashRows.Select(r => r.PeriodDate.ToString(labelFormat)).ToList()
        : new List<string>();

    var cashOnline = hasCashRows ? cashRows.Select(r => r.OnlineAmount).ToList() : new List<decimal>();
    var cashCod = hasCashRows ? cashRows.Select(r => r.CodAmount).ToList() : new List<decimal>();
    var cashTotal = hasCashRows ? cashRows.Select(r => r.TotalAmount).ToList() : new List<decimal>();

    var cashLabelsJson = JsonSerializer.Serialize(cashLabels);
    var cashOnlineJson = JsonSerializer.Serialize(cashOnline);
    var cashCodJson = JsonSerializer.Serialize(cashCod);
    var cashTotalJson = JsonSerializer.Serialize(cashTotal);

    var cashProviders = Model.CashflowProviders ?? new List<CashflowByProviderRow>();
    var hasCashProviders = cashProviders.Any();

    // ==========================================================
    // ===== PIPELINE (đơn chưa hoàn tất) =====
    // ==========================================================
    var pipe = Model.Pipeline ?? new PipelineSnapshot();
    var pipeRows = Model.PipelineByStatus ?? new List<PipelineByStatusRow>();
    var hasPipeRows = pipeRows.Any();

    // Mốc pipeline: controller truyền @as_of_excl = ToDate+1 00:00
    var pipeAsOfText = Model.ToDate.Date.AddDays(1).ToString("dd/MM/yyyy") + " 00:00";

    // ===== Map trạng thái đơn (Type=2) =====
    var orderStatusMap = new Dictionary<byte, string>
    {
        { 0, "Mới tạo" },
        { 1, "Đã xác nhận" },
        { 2, "Đang giao" },
        { 3, "Đã giao" },
        { 4, "Đã huỷ" },
        { 5, "Chờ lấy hàng" },
        { 6, "Đang giao hàng" },
        { 7, "Đã nhận hàng" },
        { 8, "Hoàn trả" },
        { 9, "Huỷ đơn" },
        { 10, "Khác" },
    };

    string GetOrderStatusName(byte st)
        => orderStatusMap.TryGetValue(st, out var name) ? name : ("Trạng thái " + st);

    string BadgeCssByStatus(byte st)
    {
        return st switch
        {
            0 => "bg-secondary",
            1 => "bg-info",
            2 => "bg-primary",
            3 => "bg-success",
            4 => "bg-danger",
            5 => "bg-secondary",
            6 => "bg-primary",
            7 => "bg-success",
            8 => "bg-warning text-dark",
            9 => "bg-danger",
            _ => "bg-light text-dark border"
        };
    }
}

<div class="cmstools-page-header mb-3">
    <div>
        <h2 class="cmstools-page-title">Báo cáo doanh thu</h2>
        <div class="cmstools-page-subtitle">
            <span>
                Khoảng ngày:
                <strong>@Model.FromDate.ToString("dd/MM/yyyy") – @Model.ToDate.ToString("dd/MM/yyyy")</strong>
            </span>
            <span class="ms-3 small text-muted">
                <strong>Doanh thu ghi nhận</strong>: theo <strong>ngày giao/nhận</strong> (delivered_at/received_confirmed_at).
                <strong>Dòng tiền (tiền vào)</strong>: Online theo <strong>paid_at</strong>, COD theo <strong>giao/nhận</strong>.
            </span>
        </div>
    </div>
</div>

<div class="card mb-3 report-filter-card">
    <div class="card-body py-2">
        <form method="get" class="row g-2 cmstools-filter-row">
            <div class="col-12 col-md-3">
                <label class="form-label small mb-1">Từ ngày</label>
                <div class="cmstools-date-wrapper">
                    <input type="text"
                           class="form-control form-control-sm js-date-display"
                           data-target-name="fromDate"
                           value="@Model.FromDate.ToString("dd/MM/yyyy")"
                           placeholder="dd/MM/yyyy"
                           autocomplete="off" />
                    <input type="hidden"
                           name="fromDate"
                           value="@Model.FromDate.ToString("yyyy-MM-dd")" />
                </div>
            </div>

            <div class="col-12 col-md-3">
                <label class="form-label small mb-1">Đến ngày</label>
                <div class="cmstools-date-wrapper">
                    <input type="text"
                           class="form-control form-control-sm js-date-display"
                           data-target-name="toDate"
                           value="@Model.ToDate.ToString("dd/MM/yyyy")"
                           placeholder="dd/MM/yyyy"
                           autocomplete="off" />
                    <input type="hidden"
                           name="toDate"
                           value="@Model.ToDate.ToString("yyyy-MM-dd")" />
                </div>
            </div>

            <div class="col-6 col-md-3">
                <label class="form-label small mb-1">Nhóm theo</label>
                <select name="groupBy" class="form-select form-select-sm">
                    @if (Model.GroupBy == "month")
                    {
                        <option value="day">Ngày</option>
                        <option value="month" selected="selected">Tháng</option>
                    }
                    else
                    {
                        <option value="day" selected="selected">Ngày</option>
                        <option value="month">Tháng</option>
                    }
                </select>
            </div>

            <div class="col-12 col-md-3 d-flex align-items-end">
                <div class="ms-md-auto text-md-end">
                    <div class="btn-group btn-group-sm mb-2 report-range-chips">
                        <button type="button" class="btn btn-outline-secondary" data-range="last7">7 ngày gần nhất</button>
                        <button type="button" class="btn btn-outline-secondary" data-range="thisMonth">Tháng này</button>
                        <button type="button" class="btn btn-outline-secondary" data-range="lastMonth">Tháng trước</button>
                    </div>
                    <div>
                        <button type="submit" class="btn btn-primary btn-sm me-2">Lọc</button>
                        <a class="btn btn-outline-secondary btn-sm"
                           href="@Url.Action("Revenue", "Reports")">Đặt lại</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

@* ==== KPI OVERVIEW (Doanh thu ghi nhận) ==== *@
<div class="row g-3 mb-3 report-kpi-row">
    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card report-kpi-main">
            <div class="card-body py-2">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="report-kpi-label">Tổng số đơn (đã giao/nhận)</div>
                        <div class="fw-bold fs-5 report-kpi-value">@Model.Overview.TotalOrders</div>
                        <div class="report-kpi-delta @OrdDeltaCss">
                            <span class="report-kpi-dot"></span>
                            @ordDeltaText (so với kỳ trước)
                        </div>
                    </div>
                    <div class="report-kpi-icon" aria-hidden="true">📦</div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Tổng tiền hàng (sub_total)</div>
                <div class="fw-bold fs-6 report-kpi-value">
                    @Model.Overview.TotalSubTotal.ToString("N0", CultureInfo.CurrentCulture) đ
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Tổng giảm giá</div>
                <div class="fw-bold fs-6 text-danger report-kpi-value">
                    @Model.Overview.TotalDiscountTotal.ToString("N0", CultureInfo.CurrentCulture) đ
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Tổng phí ship</div>
                <div class="fw-bold fs-6 report-kpi-value">
                    @Model.Overview.TotalShippingTotal.ToString("N0", CultureInfo.CurrentCulture) đ
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Tổng VAT</div>
                <div class="fw-bold fs-6 report-kpi-value">
                    @Model.Overview.TotalVatTotal.ToString("N0", CultureInfo.CurrentCulture) đ
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card report-kpi-main">
            <div class="card-body py-2">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="report-kpi-label">Doanh thu ghi nhận (pay_total)</div>
                        <div class="fw-bold fs-5 text-success report-kpi-value">
                            @Model.Overview.TotalPayTotal.ToString("N0", CultureInfo.CurrentCulture) đ
                        </div>
                        <div class="report-kpi-delta @RevDeltaCss">
                            <span class="report-kpi-dot"></span>
                            @revDeltaText (so với kỳ trước)
                        </div>
                    </div>
                    <div class="report-kpi-icon" aria-hidden="true">💰</div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Giá trị đơn hàng TB</div>
                <div class="fw-bold fs-6 report-kpi-value">
                    @Model.Overview.AverageOrderValue.ToString("N0", CultureInfo.CurrentCulture) đ
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">@periodLabel</div>
                <div class="fw-bold fs-6 report-kpi-value">@periodCount</div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Doanh thu TB/ngày</div>
                <div class="fw-bold fs-6 report-kpi-value">
                    @avgRevenuePerDay.ToString("N0", CultureInfo.CurrentCulture) đ
                </div>
            </div>
        </div>
    </div>

    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm report-kpi-card">
            <div class="card-body py-2">
                <div class="report-kpi-label">Tỷ lệ giảm giá</div>
                <div class="fw-bold fs-6 @(discountRate > 0 ? "text-danger" : "") report-kpi-value">
                    @discountRate.ToString("0.##", CultureInfo.CurrentCulture) %
                </div>
            </div>
        </div>
    </div>

    @if (hasProviderRows)
    {
        <div class="col-6 col-md-3">
            <div class="card border-0 shadow-sm report-kpi-card">
                <div class="card-body py-2">
                    <div class="report-kpi-label">Doanh thu Online (đã giao/nhận)</div>
                    <div class="fw-bold fs-6 report-kpi-value">
                        @onlineAmount.ToString("N0", CultureInfo.CurrentCulture) đ
                    </div>
                    <div class="small text-muted">
                        @onlineRevenueShare.ToString("0.##", CultureInfo.CurrentCulture)% tổng doanh thu
                    </div>
                </div>
            </div>
        </div>

        <div class="col-6 col-md-3">
            <div class="card border-0 shadow-sm report-kpi-card">
                <div class="card-body py-2">
                    <div class="report-kpi-label">Tỷ lệ đơn Online (đã giao/nhận)</div>
                    <div class="fw-bold fs-6 report-kpi-value">
                        @onlineOrderShare.ToString("0.##", CultureInfo.CurrentCulture)% (@onlineOrders / @totalOrders đơn)
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@* ==== DÒNG TIỀN + PIPELINE ==== *@
<div class="row g-3 mb-3">
    <div class="col-12 col-lg-6">
        <div class="card border-0 shadow-sm">
            <div class="card-body py-2">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="fw-semibold">Dòng tiền (tiền vào) trong khoảng lọc</div>
                        <div class="small text-muted">
                            Online theo <code>paid_at</code>, COD theo <code>giao/nhận</code>
                        </div>
                    </div>
                    <span class="badge bg-light text-muted border">Dòng tiền</span>
                </div>

                <div class="row g-2 mt-1">
                    <div class="col-6">
                        <div class="small text-muted">Online đã thu</div>
                        <div class="fw-bold text-success">
                            @cashOv.OnlineCashIn.ToString("N0", CultureInfo.CurrentCulture) đ
                        </div>
                        <div class="small text-muted">@cashOv.OnlineOrders đơn</div>
                    </div>
                    <div class="col-6">
                        <div class="small text-muted">COD đã thu</div>
                        <div class="fw-bold">
                            @cashOv.CodCashIn.ToString("N0", CultureInfo.CurrentCulture) đ
                        </div>
                        <div class="small text-muted">@cashOv.CodOrders đơn</div>
                    </div>
                    <div class="col-12">
                        <hr class="my-2" />
                        <div class="d-flex justify-content-between">
                            <div class="small text-muted">Tổng tiền vào</div>
                            <div class="fw-bold">
                                @cashOv.TotalCashIn.ToString("N0", CultureInfo.CurrentCulture) đ
                                <span class="small text-muted">(@cashOv.TotalOrders đơn)</span>
                            </div>
                        </div>
                    </div>
                </div>

                @if (!hasCashRows)
                {
                    <div class="small text-muted mt-2">Không có dòng tiền trong khoảng lọc.</div>
                }
            </div>
        </div>
    </div>

    <div class="col-12 col-lg-6">
        <div class="card border-0 shadow-sm">
            <div class="card-body py-2">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="fw-semibold">Đơn đang xử lý (Pipeline)</div>
                        <div class="small text-muted">Mốc tính đến: <strong>@pipeAsOfText</strong> (không bao gồm mốc này)</div>
                    </div>
                    <span class="badge bg-light text-muted border">Đơn đang xử lý</span>
                </div>

                <div class="row g-2 mt-1">
                    <div class="col-4">
                        <div class="small text-muted">COD chưa thu</div>
                        <div class="fw-bold">
                            @pipe.CodOutstandingAmount.ToString("N0", CultureInfo.CurrentCulture) đ
                        </div>
                        <div class="small text-muted">@pipe.CodOutstandingOrders đơn</div>
                    </div>
                    <div class="col-4">
                        <div class="small text-muted">Online chưa thanh toán</div>
                        <div class="fw-bold text-warning">
                            @pipe.OnlinePendingAmount.ToString("N0", CultureInfo.CurrentCulture) đ
                        </div>
                        <div class="small text-muted">@pipe.OnlinePendingOrders đơn</div>
                    </div>
                    <div class="col-4">
                        <div class="small text-muted">Online đã thanh toán (chưa giao)</div>
                        <div class="fw-bold text-primary">
                            @pipe.OnlinePrepaidAmount.ToString("N0", CultureInfo.CurrentCulture) đ
                        </div>
                        <div class="small text-muted">@pipe.OnlinePrepaidOrders đơn</div>
                    </div>

                    <div class="col-12">
                        <hr class="my-2" />
                        <div class="d-flex justify-content-between">
                            <div class="small text-muted">Tổng đơn đang xử lý</div>
                            <div class="fw-bold">
                                @pipe.OpenAmount.ToString("N0", CultureInfo.CurrentCulture) đ
                                <span class="small text-muted">(@pipe.OpenOrders đơn)</span>
                            </div>
                        </div>
                        <div class="small text-muted">
                            * “Đang xử lý” = chưa giao/nhận và chưa huỷ.
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@* ==== AI ==== *@
<div class="card mb-3" id="aiCard">
    <div class="card-body">
        <div class="d-flex align-items-center justify-content-between mb-2">
            <div class="fw-semibold">Phân tích AI</div>
            <span class="badge bg-light text-muted border">Thử nghiệm</span>
        </div>

        <div class="d-flex gap-2 align-items-center mb-2">
            <button type="button" class="btn btn-outline-primary btn-sm" id="btnAiRun">
                Chạy phân tích
            </button>

            <button type="button" class="btn btn-outline-secondary btn-sm" id="btnAiForce" style="display:none;">
                Chạy lại (bỏ lưu tạm)
            </button>

            <div class="small text-muted" id="aiHint">
                (Chỉ chạy khi bạn bấm nút. Kết quả được lưu tạm 10–30 phút.)
            </div>
        </div>

        <div id="aiLoading" class="text-muted small" style="display:none;">Đang phân tích…</div>
        <div id="aiError" class="text-danger small" style="display:none;"></div>

        <div id="aiContent" style="display:none;">
            <div id="aiSummary" class="mb-2"></div>

            <div id="aiHighlightsWrap" style="display:none;">
                <div class="small text-muted fw-semibold">Điểm nổi bật</div>
                <ul id="aiHighlights" class="mb-2"></ul>
            </div>

            <div id="aiAlertsWrap" style="display:none;">
                <div class="small text-muted fw-semibold">Cảnh báo</div>
                <ul id="aiAlerts" class="mb-0"></ul>
            </div>
        </div>
    </div>
</div>

@if (hasRows)
{
    <div class="card mb-3 report-chart-card">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <div>
                    <div class="report-section-title">Xu hướng theo thời gian</div>
                    <h5 class="card-title mb-0">Doanh thu & số đơn (đã giao/nhận)</h5>
                    <span class="badge report-badge-soft mt-1">
                        Nhóm theo: @(Model.GroupBy == "month" ? "Tháng" : "Ngày")
                    </span>
                </div>

                <div class="btn-group btn-group-sm">
                    <a class="btn btn-outline-secondary"
                       href="@Url.Action("RevenueExport", "Reports", new {
                             fromDate = Model.FromDate.ToString("yyyy-MM-dd"),
                             toDate   = Model.ToDate.ToString("yyyy-MM-dd"),
                             groupBy  = Model.GroupBy,
                             format   = "csv"
                       })">Xuất CSV</a>
                    <a class="btn btn-outline-secondary"
                       href="@Url.Action("RevenueExport", "Reports", new {
                             fromDate = Model.FromDate.ToString("yyyy-MM-dd"),
                             toDate   = Model.ToDate.ToString("yyyy-MM-dd"),
                             groupBy  = Model.GroupBy,
                             format   = "excel"
                       })">Xuất Excel</a>
                </div>
            </div>

            <div style="max-height: 320px;">
                <canvas id="revenueChart"></canvas>
            </div>
        </div>
    </div>
}

@* ==== BIỂU ĐỒ DÒNG TIỀN ==== *@
@if (hasCashRows)
{
    <div class="card mb-3 report-chart-card">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <div>
                    <div class="report-section-title">Dòng tiền</div>
                    <h5 class="card-title mb-0">Tiền vào (Online theo paid_at, COD theo giao/nhận)</h5>
                    <span class="badge report-badge-soft mt-1">
                        Nhóm theo: @(Model.GroupBy == "month" ? "Tháng" : "Ngày")
                    </span>
                </div>
            </div>

            <div style="max-height: 320px;">
                <canvas id="cashflowChart"></canvas>
            </div>
        </div>
    </div>
}

@* ==== BẢNG DÒNG TIỀN THEO KÊNH ==== *@
@if (hasCashProviders)
{
    <div class="card mb-3">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <h5 class="card-title mb-0 small text-muted">Dòng tiền theo kênh thanh toán</h5>
                <span class="badge bg-light text-muted border">Dựa trên “tiền vào” trong khoảng lọc</span>
            </div>

            <div class="cmstools-table-container">
                <div class="cmstools-table-scroll">
                    <table class="table table-sm table-bordered align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Kênh</th>
                                <th class="text-end">Số đơn</th>
                                <th class="text-end">Tiền vào</th>
                                <th class="text-end">TB/đơn</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var p in cashProviders)
                            {
                                var prov = (p.ProviderLabel ?? "").Trim();
                                var meth = (p.MethodLabel ?? "").Trim();
                                var avg = p.SuccessfulOrders > 0 ? (p.TotalAmount / p.SuccessfulOrders) : 0m;

                                <tr>
                                    <td>
                                        <strong>@(string.IsNullOrWhiteSpace(prov) ? "Không rõ" : prov)</strong>
                                        @if (!string.IsNullOrWhiteSpace(meth) && !string.Equals(meth, prov, StringComparison.OrdinalIgnoreCase))
                                        {
                                            <div class="small text-muted">@meth</div>
                                        }
                                    </td>
                                    <td class="text-end">@p.SuccessfulOrders</td>
                                    <td class="text-end fw-semibold">@p.TotalAmount.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                    <td class="text-end">@avg.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
}

@if (!hasRows)
{
    <p class="text-muted">Không có dữ liệu trong khoảng lọc.</p>
}
else
{
    @* ==== BẢNG THEO NGÀY/THÁNG (Doanh thu ghi nhận) ==== *@
    <div class="cmstools-table-container mb-3">
        <div class="cmstools-table-scroll">
            <table class="table table-striped table-sm table-bordered cmstools-table mb-0">
                <thead class="table-light">
                    <tr>
                        <th>@(Model.GroupBy == "month" ? "Tháng giao/nhận" : "Ngày giao/nhận")</th>
                        <th class="text-end">Số đơn</th>
                        <th class="text-end">SubTotal</th>
                        <th class="text-end">Giảm giá</th>
                        <th class="text-end">Phí ship</th>
                        <th class="text-end">VAT</th>
                        <th class="text-end">PayTotal</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var r in Model.Rows)
                    {
                        var isTop = topPeriodDates.Contains(r.PeriodDate.Date);

                        DateTime periodDate = r.PeriodDate.Date;
                        DateTime fromFilterDate;
                        DateTime toFilterDate;

                        if (Model.GroupBy == "month")
                        {
                            fromFilterDate = new DateTime(periodDate.Year, periodDate.Month, 1);
                            toFilterDate = fromFilterDate.AddMonths(1).AddDays(-1);
                        }
                        else
                        {
                            fromFilterDate = periodDate;
                            toFilterDate = periodDate;
                        }

                        var drillUrl = Url.Action("Index", "Orders", new {
                            deliveredFrom = fromFilterDate.ToString("yyyy-MM-dd"),
                            deliveredTo = toFilterDate.ToString("yyyy-MM-dd")
                        });

                        <tr class="@(isTop ? "table-warning" : "")">
                            <td>
                                <a href="@drillUrl" class="text-decoration-none @(isTop ? "fw-bold" : "")">
                                    @r.PeriodDate.ToString(Model.GroupBy == "month" ? "MM/yyyy" : "dd/MM/yyyy")
                                </a>
                            </td>
                            <td class="text-end">@r.OrderCount</td>
                            <td class="text-end">@r.SubTotal.ToString("N0", CultureInfo.CurrentCulture)</td>
                            <td class="text-end">@r.DiscountTotal.ToString("N0", CultureInfo.CurrentCulture)</td>
                            <td class="text-end">@r.ShippingTotal.ToString("N0", CultureInfo.CurrentCulture)</td>
                            <td class="text-end">@r.VatTotal.ToString("N0", CultureInfo.CurrentCulture)</td>
                            <td class="text-end fw-bold">@r.PayTotal.ToString("N0", CultureInfo.CurrentCulture)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    @* ==== TOP 5 ==== *@
    @if (top5RevenueDays.Any())
    {
        <div class="card mb-3">
            <div class="card-body py-2">
                <h5 class="card-title mb-2 small text-muted">Top 5 @(Model.GroupBy == "month" ? "tháng" : "ngày") có doanh thu cao nhất</h5>

                <div class="cmstools-table-container">
                    <div class="cmstools-table-scroll">
                        <table class="table table-sm table-bordered align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th style="width:60px;">#</th>
                                    <th>@(Model.GroupBy == "month" ? "Tháng giao/nhận" : "Ngày giao/nhận")</th>
                                    <th class="text-end">Số đơn</th>
                                    <th class="text-end">Doanh thu</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    var idx = 1;
                                }
                                @foreach (var r in top5RevenueDays)
                                {
                                    DateTime periodDate = r.PeriodDate.Date;
                                    DateTime fromFilterDate;
                                    DateTime toFilterDate;

                                    if (Model.GroupBy == "month")
                                    {
                                        fromFilterDate = new DateTime(periodDate.Year, periodDate.Month, 1);
                                        toFilterDate = fromFilterDate.AddMonths(1).AddDays(-1);
                                    }
                                    else
                                    {
                                        fromFilterDate = periodDate;
                                        toFilterDate = periodDate;
                                    }

                                    var drillUrl = Url.Action("Index", "Orders", new {
                                        deliveredFrom = fromFilterDate.ToString("yyyy-MM-dd"),
                                        deliveredTo = toFilterDate.ToString("yyyy-MM-dd")
                                    });

                                    <tr>
                                        <td class="text-center">@idx</td>
                                        <td>
                                            <a href="@drillUrl" class="text-decoration-none fw-semibold">
                                                @r.PeriodDate.ToString(Model.GroupBy == "month" ? "MM/yyyy" : "dd/MM/yyyy")
                                            </a>
                                        </td>
                                        <td class="text-end">@r.OrderCount</td>
                                        <td class="text-end fw-bold">@r.PayTotal.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                    </tr>
                                    idx++;
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    }

    @* ==== Cảnh báo đối soát ==== *@
    @if (providersMismatch)
    {
        <div class="alert alert-warning d-flex justify-content-between align-items-start mb-3">
            <div>
                <div class="fw-semibold">Cảnh báo đối soát theo kênh thanh toán (doanh thu ghi nhận)</div>
                <div class="small">
                    Tổng theo kênh = <strong>@providersSumAmount.ToString("N0", CultureInfo.CurrentCulture) đ</strong>
                    / @providersSumOrders đơn,
                    trong khi Tổng quan = <strong>@totalRevenue.ToString("N0", CultureInfo.CurrentCulture) đ</strong>
                    / @totalOrders đơn.
                </div>
                <div class="small text-muted">
                    Chênh lệch: @providersAmountDiff.ToString("N0", CultureInfo.CurrentCulture) đ,
                    @providersOrdersDiff đơn.
                </div>
            </div>
            <div class="small text-muted text-end">
                Gợi ý: kiểm tra đơn có retry/duplicate giao dịch, hoặc mapping kênh chưa thống nhất.
            </div>
        </div>
    }

    @* ==== KÊNH THANH TOÁN (Doanh thu ghi nhận) ==== *@
    @if (hasProviderRows)
    {
        <div class="card mb-3">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <h5 class="card-title mb-0 small text-muted">Phân bổ doanh thu theo kênh thanh toán</h5>
                    <span class="badge bg-light text-muted border">Dựa trên đơn đã giao/nhận trong khoảng lọc</span>
                </div>

                <ul class="nav nav-tabs small" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active"
                                id="tab-provider-amount"
                                data-bs-toggle="tab"
                                data-bs-target="#tabProviderAmount"
                                type="button" role="tab">
                            Tỷ trọng doanh thu
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link"
                                id="tab-provider-orders"
                                data-bs-toggle="tab"
                                data-bs-target="#tabProviderOrders"
                                type="button" role="tab">
                            Số đơn theo kênh
                        </button>
                    </li>
                </ul>

                <div class="tab-content pt-3">
                    <div class="tab-pane fade show active" id="tabProviderAmount" role="tabpanel">
                        <div class="row g-3 align-items-center">
                            <div class="col-12 col-lg-6">
                                <div style="max-height:260px;">
                                    <canvas id="providerAmountChart"></canvas>
                                </div>
                            </div>
                            <div class="col-12 col-lg-6">
                                <div class="cmstools-table-container">
                                    <div class="cmstools-table-scroll">
                                        <table class="table table-sm table-bordered align-middle mb-0">
                                            <thead class="table-light">
                                                <tr>
                                                    <th>Kênh</th>
                                                    <th class="text-end">Số đơn</th>
                                                    <th class="text-end">Doanh thu</th>
                                                    <th class="text-end">TB/đơn</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @foreach (var p in providerRows)
                                                {
                                                    var avg = p.SuccessfulOrders > 0 ? (p.TotalAmount / p.SuccessfulOrders) : 0m;
                                                    <tr>
                                                        <td>
                                                            <strong>@p.ProviderLabel</strong>
                                                            <div class="small text-muted">@p.MethodLabel</div>
                                                        </td>
                                                        <td class="text-end">@p.SuccessfulOrders</td>
                                                        <td class="text-end">@p.TotalAmount.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                                        <td class="text-end">@avg.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                                    </tr>
                                                }
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="tab-pane fade" id="tabProviderOrders" role="tabpanel">
                        <div class="row g-3 align-items-center">
                            <div class="col-12 col-lg-6">
                                <div style="max-height:260px;">
                                    <canvas id="providerOrdersChart"></canvas>
                                </div>
                            </div>
                            <div class="col-12 col-lg-6">
                                <p class="small text-muted mb-1">
                                    Biểu đồ thể hiện tỷ lệ số đơn theo từng kênh.
                                </p>
                                <ul class="small mb-0">
                                    @foreach (var p in providerRows)
                                    {
                                        <li><strong>@p.ProviderLabel</strong> (@p.MethodLabel): @p.SuccessfulOrders đơn</li>
                                    }
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    }

    @* ==== PIPELINE THEO TRẠNG THÁI ==== *@
    @if (hasPipeRows)
    {
        <div class="card mb-3">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <h5 class="card-title mb-0 small text-muted">Đơn đang xử lý theo trạng thái</h5>
                    <span class="badge bg-light text-muted border">Tính đến @pipeAsOfText</span>
                </div>

                <div class="cmstools-table-container">
                    <div class="cmstools-table-scroll">
                        <table class="table table-sm table-bordered align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th style="width:240px;">Trạng thái</th>
                                    <th class="text-end">Số đơn</th>
                                    <th class="text-end">Tổng tiền</th>
                                    <th class="text-end">COD chưa thu</th>
                                    <th class="text-end">Online chưa thanh toán</th>
                                    <th class="text-end">Online đã thanh toán</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var r in pipeRows.OrderBy(x => x.Status))
                                {
                                    var stName = GetOrderStatusName(r.Status);
                                    var badge = BadgeCssByStatus(r.Status);

                                    <tr>
                                        <td>
                                            <span class="badge @badge me-2">@r.Status</span>
                                            <span class="fw-semibold">@stName</span>
                                        </td>
                                        <td class="text-end">@r.OrderCount</td>
                                        <td class="text-end fw-semibold">@r.Amount.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                        <td class="text-end">@r.CodAmount.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                        <td class="text-end">@r.OnlinePendingAmount.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                        <td class="text-end">@r.OnlinePrepaidAmount.ToString("N0", CultureInfo.CurrentCulture) đ</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot class="table-light">
                                <tr>
                                    <th>Tổng</th>
                                    <th class="text-end">@pipeRows.Sum(x => x.OrderCount)</th>
                                    <th class="text-end">@pipeRows.Sum(x => x.Amount).ToString("N0", CultureInfo.CurrentCulture) đ</th>
                                    <th class="text-end">@pipeRows.Sum(x => x.CodAmount).ToString("N0", CultureInfo.CurrentCulture) đ</th>
                                    <th class="text-end">@pipeRows.Sum(x => x.OnlinePendingAmount).ToString("N0", CultureInfo.CurrentCulture) đ</th>
                                    <th class="text-end">@pipeRows.Sum(x => x.OnlinePrepaidAmount).ToString("N0", CultureInfo.CurrentCulture) đ</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>

                <div class="small text-muted mt-2">
                    Gợi ý: click vào biểu đồ “Doanh thu & số đơn” để drilldown danh sách đơn theo ngày/tháng.
                </div>
            </div>
        </div>
    }
}

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        (function () {
            "use strict";

            function pad2(n) { return n < 10 ? "0" + n : "" + n; }
            function formatDisplayDate(d) { return pad2(d.getDate()) + "/" + pad2(d.getMonth() + 1) + "/" + d.getFullYear(); }
            function formatHiddenDate(d) { return d.getFullYear() + "-" + pad2(d.getMonth() + 1) + "-" + pad2(d.getDate()); }

            function parseDisplayDate(str) {
                if (!str) return null;
                var m = /^(\d{1,2})\/(\d{1,2})\/(\d{4})$/.exec(str.trim());
                if (!m) return null;
                var dd = parseInt(m[1], 10);
                var MM = parseInt(m[2], 10);
                var yyyy = parseInt(m[3], 10);
                if (MM < 1 || MM > 12 || dd < 1 || dd > 31) return null;
                var d = new Date(yyyy, MM - 1, dd);
                if (d.getFullYear() !== yyyy || d.getMonth() !== MM - 1 || d.getDate() !== dd) return null;
                return d;
            }

            function syncDisplayToHidden(input) {
                var wrapper = input.closest(".cmstools-date-wrapper");
                if (!wrapper) return;
                var targetName = input.getAttribute("data-target-name");
                if (!targetName) return;
                var hidden = wrapper.querySelector('input[type="hidden"][name="' + targetName + '"]');
                if (!hidden) return;

                var dt = parseDisplayDate(input.value);
                if (!dt) {
                    hidden.value = "";
                    input.classList.add("is-invalid");
                } else {
                    hidden.value = formatHiddenDate(dt);
                    input.value = formatDisplayDate(dt);
                    input.classList.remove("is-invalid");
                }
            }

            document.addEventListener("blur", function (e) {
                var input = e.target;
                if (input.classList && input.classList.contains("js-date-display")) {
                    syncDisplayToHidden(input);
                }
            }, true);

            function setRange(range) {
                var form = document.querySelector(".cmstools-filter-row");
                if (!form) return;
                form = form.closest("form");
                if (!form) return;

                var fromDisplay = form.querySelector('.js-date-display[data-target-name="fromDate"]');
                var toDisplay   = form.querySelector('.js-date-display[data-target-name="toDate"]');
                var fromHidden  = form.querySelector('input[type="hidden"][name="fromDate"]');
                var toHidden    = form.querySelector('input[type="hidden"][name="toDate"]');
                if (!fromDisplay || !toDisplay || !fromHidden || !toHidden) return;

                var today = new Date();
                today.setHours(0, 0, 0, 0);

                var from, to;
                if (range === "last7") {
                    to = new Date(today);
                    from = new Date(today); from.setDate(from.getDate() - 6);
                } else if (range === "thisMonth") {
                    to = new Date(today);
                    from = new Date(today.getFullYear(), today.getMonth(), 1);
                } else if (range === "lastMonth") {
                    var firstThisMonth = new Date(today.getFullYear(), today.getMonth(), 1);
                    var lastMonthEnd = new Date(firstThisMonth.getTime() - 86400000);
                    var lastMonthStart = new Date(lastMonthEnd.getFullYear(), lastMonthEnd.getMonth(), 1);
                    from = lastMonthStart; to = lastMonthEnd;
                } else return;

                fromDisplay.value = formatDisplayDate(from);
                toDisplay.value   = formatDisplayDate(to);
                fromHidden.value  = formatHiddenDate(from);
                toHidden.value    = formatHiddenDate(to);

                form.submit();
            }

            document.addEventListener("click", function (e) {
                var btn = e.target.closest("[data-range]");
                if (!btn) return;
                var range = btn.getAttribute("data-range");
                if (!range) return;
                e.preventDefault();
                setRange(range);
            });

            // ===== Chart.js (Doanh thu ghi nhận) =====
            var hasRows = @hasRows.ToString().ToLower();
            var groupBy = "@Model.GroupBy";

            function buildOrdersUrl(fromStr, toStr) {
                var baseUrl = '@Url.Action("Index", "Orders")';
                return baseUrl + "?deliveredFrom=" + encodeURIComponent(fromStr)
                               + "&deliveredTo=" + encodeURIComponent(toStr);
            }

            function parseLabelToRange(label) {
                if (groupBy === "month") {
                    var partsM = label.split("/");
                    if (partsM.length !== 2) return null;
                    var mm = parseInt(partsM[0], 10);
                    var yyyy = parseInt(partsM[1], 10);
                    if (!mm || !yyyy) return null;
                    var from = new Date(yyyy, mm - 1, 1);
                    var to = new Date(yyyy, mm, 0);
                    return { from: formatHiddenDate(from), to: formatHiddenDate(to) };
                } else {
                    var partsD = label.split("/");
                    if (partsD.length !== 3) return null;
                    var dd = parseInt(partsD[0], 10);
                    var mm2 = parseInt(partsD[1], 10);
                    var yyyy2 = parseInt(partsD[2], 10);
                    if (!dd || !mm2 || !yyyy2) return null;
                    var d = new Date(yyyy2, mm2 - 1, dd);
                    return { from: formatHiddenDate(d), to: formatHiddenDate(d) };
                }
            }

            if (hasRows) {
                const labels      = @Html.Raw(labelsJson);
                const revenueData = @Html.Raw(revenueJson);
                const ordersData  = @Html.Raw(ordersJson);

                const ctx = document.getElementById("revenueChart");
                if (ctx) {
                    const chart = new Chart(ctx, {
                        type: "bar",
                        data: {
                            labels: labels,
                            datasets: [
                                { type: "line", label: "Doanh thu (đ)", data: revenueData, yAxisID: "yRevenue", borderWidth: 2, tension: 0.3, pointRadius: 3 },
                                { type: "bar",  label: "Số đơn",        data: ordersData,  yAxisID: "yOrders",  borderWidth: 1 }
                            ]
                        },
                        options: {
                            responsive: true,
                            maintainAspectRatio: false,
                            interaction: { mode: "index", intersect: false },
                            stacked: false,
                            scales: {
                                yRevenue: { type: "linear", display: true, position: "left", title: { display: true, text: "Doanh thu (đ)" } },
                                yOrders:  { type: "linear", display: true, position: "right", grid: { drawOnChartArea: false }, title: { display: true, text: "Số đơn" }, ticks: { precision: 0 } },
                                x: { ticks: { maxRotation: 45, minRotation: 0 } }
                            },
                            plugins: {
                                legend: { display: true },
                                tooltip: {
                                    callbacks: {
                                        label: function (context) {
                                            if (context.dataset.yAxisID === "yRevenue") {
                                                const v = context.parsed.y || 0;
                                                return " Doanh thu: " + v.toLocaleString("vi-VN") + " đ";
                                            } else {
                                                return " Số đơn: " + context.parsed.y;
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    });

                    ctx.onclick = function (evt) {
                        const points = chart.getElementsAtEventForMode(evt, "nearest", { intersect: true }, true);
                        if (!points.length) return;
                        const idx = points[0].index;
                        const label = chart.data.labels[idx];
                        const range = parseLabelToRange(label);
                        if (!range) return;
                        window.location.href = buildOrdersUrl(range.from, range.to);
                    };
                }
            }

            // ===== Biểu đồ dòng tiền =====
            var hasCashRows = @hasCashRows.ToString().ToLower();
            if (hasCashRows) {
                const cLabels = @Html.Raw(cashLabelsJson);
                const cOnline = @Html.Raw(cashOnlineJson);
                const cCod    = @Html.Raw(cashCodJson);
                const cTotal  = @Html.Raw(cashTotalJson);

                const ctxC = document.getElementById("cashflowChart");
                if (ctxC) {
                    new Chart(ctxC, {
                        type: "bar",
                        data: {
                            labels: cLabels,
                            datasets: [
                                { type: "line", label: "Online đã thu", data: cOnline, yAxisID: "yCash", borderWidth: 2, tension: 0.3, pointRadius: 3 },
                                { type: "bar",  label: "COD đã thu",    data: cCod,    yAxisID: "yCash", borderWidth: 1 },
                                { type: "line", label: "Tổng tiền vào", data: cTotal,  yAxisID: "yCash", borderWidth: 2, tension: 0.3, pointRadius: 0 }
                            ]
                        },
                        options: {
                            responsive: true,
                            maintainAspectRatio: false,
                            interaction: { mode: "index", intersect: false },
                            scales: {
                                yCash: { type: "linear", display: true, position: "left", title: { display: true, text: "Tiền vào (đ)" } },
                                x: { ticks: { maxRotation: 45, minRotation: 0 } }
                            },
                            plugins: {
                                legend: { display: true },
                                tooltip: {
                                    callbacks: {
                                        label: function (context) {
                                            const v = context.parsed.y || 0;
                                            return " " + context.dataset.label + ": " + v.toLocaleString("vi-VN") + " đ";
                                        }
                                    }
                                }
                            }
                        }
                    });
                }
            }

            // ===== Biểu đồ kênh thanh toán (doanh thu ghi nhận) =====
            var hasProviderRows = @hasProviderRows.ToString().ToLower();
            if (hasProviderRows) {
                const providerLabels  = @Html.Raw(providerLabelsJson);
                const providerAmounts = @Html.Raw(providerAmountsJson);
                const providerOrders  = @Html.Raw(providerOrdersJson);

                const ctxAmount = document.getElementById("providerAmountChart");
                if (ctxAmount) {
                    new Chart(ctxAmount, {
                        type: "doughnut",
                        data: { labels: providerLabels, datasets: [{ label: "Doanh thu (đ)", data: providerAmounts }] },
                        options: {
                            responsive: true,
                            plugins: {
                                legend: { position: "bottom" },
                                tooltip: {
                                    callbacks: {
                                        label: function (context) {
                                            const label = context.label || "";
                                            const value = context.parsed || 0;
                                            return label + ": " + value.toLocaleString("vi-VN") + " đ";
                                        }
                                    }
                                }
                            }
                        }
                    });
                }

                const ctxOrders = document.getElementById("providerOrdersChart");
                if (ctxOrders) {
                    new Chart(ctxOrders, {
                        type: "doughnut",
                        data: { labels: providerLabels, datasets: [{ label: "Số đơn", data: providerOrders }] },
                        options: {
                            responsive: true,
                            plugins: {
                                legend: { position: "bottom" },
                                tooltip: {
                                    callbacks: {
                                        label: function (context) {
                                            const label = context.label || "";
                                            const value = context.parsed || 0;
                                            return label + ": " + value + " đơn";
                                        }
                                    }
                                }
                            }
                        }
                    });
                }
            }
        })();
    </script>

    <script>
(function () {
  "use strict";

  const btnRun = document.getElementById("btnAiRun");
  const btnForce = document.getElementById("btnAiForce");
  const loading = document.getElementById("aiLoading");
  const errBox = document.getElementById("aiError");

  const content = document.getElementById("aiContent");
  const summaryEl = document.getElementById("aiSummary");

  const hiWrap = document.getElementById("aiHighlightsWrap");
  const hiUl = document.getElementById("aiHighlights");

  const alWrap = document.getElementById("aiAlertsWrap");
  const alUl = document.getElementById("aiAlerts");

  const fromStr = "@Model.FromDate.ToString("yyyy-MM-dd")";
  const toStr   = "@Model.ToDate.ToString("yyyy-MM-dd")";
  const groupBy = "@Model.GroupBy";

  function setBusy(isBusy) {
    btnRun.disabled = isBusy;
    loading.style.display = isBusy ? "block" : "none";
  }

  function resetUi() {
    errBox.style.display = "none";
    errBox.textContent = "";

    content.style.display = "none";
    summaryEl.textContent = "";

    hiWrap.style.display = "none";
    hiUl.innerHTML = "";

    alWrap.style.display = "none";
    alUl.innerHTML = "";
  }

  function render(ai, cached) {
    resetUi();

    if (!ai) {
      errBox.textContent = "Không có dữ liệu AI.";
      errBox.style.display = "block";
      return;
    }

    btnForce.style.display = "inline-block";

    summaryEl.textContent = ai.summary || "";
    if (cached) {
      const span = document.createElement("span");
      span.className = "badge bg-light text-muted border ms-2";
      span.textContent = "đã lưu tạm";
      summaryEl.appendChild(span);
    }

    if (ai.highlights && ai.highlights.length) {
      hiWrap.style.display = "block";
      hiUl.innerHTML = ai.highlights.map(x => `<li>${escapeHtml(x)}</li>`).join("");
    }

    if (ai.alerts && ai.alerts.length) {
      alWrap.style.display = "block";
      alUl.innerHTML = ai.alerts.map(x => `<li>${escapeHtml(x)}</li>`).join("");
    }

    content.style.display = "block";
  }

  async function runAi(force) {
    resetUi();
    setBusy(true);

    try {
      const url = new URL("@Url.Action("RevenueAi", "Reports")", window.location.origin);
      url.searchParams.set("fromDate", fromStr);
      url.searchParams.set("toDate", toStr);
      url.searchParams.set("groupBy", groupBy);
      if (force) url.searchParams.set("force", "true");

      const res = await fetch(url.toString(), { method: "GET" });
      const json = await res.json();

      if (!json || json.ok !== true) {
        const msg = (json && json.message) ? json.message : "AI không phản hồi.";
        errBox.textContent = msg;
        errBox.style.display = "block";
        return;
      }

      render(json.data, json.cached === true);
    } catch (e) {
      errBox.textContent = "Lỗi tải AI: " + (e && e.message ? e.message : e);
      errBox.style.display = "block";
    } finally {
      setBusy(false);
    }
  }

  if (btnRun) btnRun.addEventListener("click", function () { runAi(false); });
  if (btnForce) btnForce.addEventListener("click", function () { runAi(true); });

  function escapeHtml(s) {
    s = (s ?? "").toString();
    return s.replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  }
})();
</script>
}
