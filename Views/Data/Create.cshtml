@model CmsTools.Models.DataCreateViewModel
@using CmsTools.Models

@{
    var title = "Thêm bản ghi - " + (Model.Table.DisplayName ?? Model.Table.TableName);
    ViewData["Title"] = title;
    var backUrl = Url.Action("List", "Data", new { tableId = Model.Table.Id }) ?? "#";
}

<h2 class="mt-3 mb-3">@title</h2>

<div class="card">
    <div class="card-body">
        @Html.Partial("_CreateForm", Model)
    </div>
</div>

<div class="mt-3">
    <a class="btn btn-secondary"
       href="@backUrl">
        Quay lại danh sách
    </a>
</div>
