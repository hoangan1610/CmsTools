@model CmsTools.Models.CmsRoleEditViewModel

@{
    ViewData["Title"] = "Sửa role";
}

<h2 class="mt-3 mb-3">Sửa role</h2>

<form asp-action="Edit" asp-controller="Roles" asp-route-id="@Model.Id" method="post">
    @Html.AntiForgeryToken()

    <div class="mb-3">
        <label class="form-label">ID</label>
        <input class="form-control" value="@Model.Id" readonly />
    </div>

    <div class="mb-3">
        <label asp-for="Name" class="form-label">Tên role</label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Description" class="form-label">Mô tả</label>
        <textarea asp-for="Description" class="form-control" rows="2"></textarea>
    </div>

    <div class="form-check mb-3">
        <input asp-for="IsActive" class="form-check-input" />
        <label asp-for="IsActive" class="form-check-label">Active</label>
    </div>

    <button type="submit" class="btn btn-primary">Lưu</button>
    <a class="btn btn-secondary ms-2" href="@Url.Action("Index", "Roles")">Quay lại</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
